// *************
// Challenge #1
// *************
/*
// Test Data 1
let massMark = 78;
let heightMark = 1.69;
let massJohn = 92;
let heightJohn = 1.95;
let markHigherBMI;

let bmiMark = massMark / heightMark ** 2;
let bmiJohn = massJohn / heightJohn ** 2;
const bmiMarkSecond = massMark / (heightMark * heightMark);
const bmiJohnSecond = massJohn / (heightJohn * heightJohn);
markHigherBMI = bmiMark > bmiJohn;

console.log("Mark's BMI: " + Math.round(bmiMark * 10) / 10);
console.log("John's BMI: " + Math.round(bmiJohn * 10) / 10);
console.log(
  "Mark's BMI using second formula: " + Math.round(bmiMarkSecond * 10) / 10
);
console.log(
  "John's BMI using second formula: " + Math.round(bmiJohnSecond * 10) / 10
);
console.log("Does Mark have higher BMI than John?: " + markHigherBMI);

// Test Data 2
massMark = 95;
heightMark = 1.88;
massJohn = 85;
heightJohn = 1.76;

bmiMark = massMark / heightMark ** 2;
bmiJohn = massJohn / heightJohn ** 2;
markHigherBMI = bmiMark > bmiJohn;

console.log("Mark's BMI: " + Math.round(bmiMark * 10) / 10);
console.log("John's BMI: " + Math.round(bmiJohn * 10) / 10);
console.log("Does Mark have higher BMI than John?: " + markHigherBMI);
*/
